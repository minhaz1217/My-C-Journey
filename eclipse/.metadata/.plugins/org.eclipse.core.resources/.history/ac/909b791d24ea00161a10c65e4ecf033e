#include<bits/stdc++.h>
using namespace std;
#define check(a) cout<<a<< endl;
int main(){
    double tc,a,b,ans;
    cin >> tc;
    while(tc--){
        cin >> a >> b;
        a -= 2;
        b-=2;
        ans = ceil(a/3) * ceil(b/3);
        cout << ans << endl;
    }
	return 0;
}
