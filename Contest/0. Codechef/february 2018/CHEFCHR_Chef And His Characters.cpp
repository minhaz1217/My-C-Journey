#include<bits/stdc++.h>
//#include<iostream>
using namespace std;
#define check(a) cout << a << endl;
#define cc(a) cout << a << endl;
#define msg(a,b) cout << a << " : " << b << endl;
#define msg2(a,b,c) cout << a << " : " << b << " : " << c << endl;

int main(){

    string temp, str;
    int tc,i;
    cin >> tc;
    while(tc--){
        cin >> str;
        cout << str << endl;
            //sort(temp[i]+0, temp[i]+4);
            msg(i, temp)
        for(i=0;i<str.size()-3;i++){
            for(j=0;j<4;j++){
                if(arr[j] == 'c')
            }
            msg(i, temp)
        }


    }




    return 0;
}
