#include<bits/stdc++.h>
using namespace std;
#define check(a) cout << a << endl;
#define msg(a,b) cout << a << " : " << b << endl;

#define MX 20
int des[20];

int findCost(int arr[],int sizes, int mm){
    int secondArr[20] = {0};
    for(i=0;i< sizes;i++){
        k = arr[i];
        if()
    }
}



int main(){
    int tc,n,m,i, small;
    cin >> tc;
    while(tc--){
        cin >> n >> m;
        int arr[n+1];
        for(i=0;i<n;i++){
            cin >> arr[i];
        }
        for(i=0;i<MX;i++){
            des[i] = 1;
        }

        sort(arr, arr+n);
        small = m/1;
    }

    return 0;
}
